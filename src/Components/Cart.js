import React from 'react';
import CartContainer from './CartContainer';
function Cart({current_cart, cart, products}) {


    // if (current_cart.products.first.exist) {
    // if (cart.last.products.first.exist) {

    // return (
    //         <div className="about-us">
    //         <CartContainer setCart={current_cart}/>
    //         <button type="button" className="card-button-shop-all">Checkout</button>
    //         </div>)
            
    //         } else {

    return (
        <div className="about-us">
            <h1>Shopping Cart</h1>
            <p>The world is quiet here...</p>  
        </div>)
    // };
};





export default Cart;